import React from "react";
import styled from "styled-components";

const ChannelList = () => {
  return <ChannelDiv>Channellist</ChannelDiv>;
};

const ChannelDiv = styled.div`
  background-color: #2f3136;
  color: white;
  width: 20%;
`;

export default ChannelList;
